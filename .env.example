# CodeMonkey Games Launcher — Example environment variables
#
# Copy this file to `.env` and customize as needed. If you don't use
# `deno run --env-file=.env` (or equivalent), set these via your shell/CI.

# HTTP port for the launcher server
PORT=8000

# Game library directory
# - Dev default: repo-local `./games`
# - Compiled Windows exe default: next to the exe: `<exe_dir>\games`
# Set to override the default location.
# Example (Windows):
# CMG_GAMES_DIR=C:\Users\you\AppData\Local\cmg\games
CMG_GAMES_DIR=

# Auto-launch kiosk browser when running the compiled Windows binary
# 1 = enabled (default), 0 = disabled
CMG_AUTO_KIOSK=1

# Disable all Chrome/Edge extensions when launching kiosk (e.g., Phaser Debugger)
# 1 = disable (default), 0 = allow extensions
CMG_DISABLE_EXTENSIONS=1

# Optional: Force a specific browser user data directory for kiosk runs
# When CMG_DISABLE_EXTENSIONS=1 and this is not set, the app creates a temporary
# clean profile to ensure no extensions load. Set this to persist a clean profile.
# Example: C:\\Users\\you\\AppData\\Local\\cmg\\browser-profile
CMG_BROWSER_DATA_DIR=
